export const enum ROUTES {
  HOME = "/",
  COMPONENTS = "/components",
  CONSTANTS = "/constants",
  CONTEXTS = "/contexts",
  UTILS = "/utils",
}

export const enum PAGE_LABELS {
  HOME = "Home",
  COMPONENTS = "Components",
  CONSTANTS = "Constants",
  CONTEXTS = "Contexts",
  UTILS = "Utils",
}
